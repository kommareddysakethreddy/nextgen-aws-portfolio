version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p assets/js
        - echo "window.__CONFIG__ = { API_BASE: '${API_BASE}' };" > assets/js/config.js
  artifacts:
    baseDirectory: .
    files:
      - "**/*"
  cache:
    paths: []
