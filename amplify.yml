version: 1
frontend:
  phases:
    build:
      commands:
        - mkdir -p assets/js
        - |
          printf "window.__CONFIG__ = { API_BASE: '%s' };\n" "$API_BASE" > assets/js/config.js
  artifacts:
    baseDirectory: .
    files:
      - "**/*"
  cache:
    paths: []
